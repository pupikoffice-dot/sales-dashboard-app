export function PlaceholderModulePage({ title }: { title: string }) {
  return (
    <div className="ov-header">
      <h2>{title}</h2>
      <p className="ov-sub">Module port from legacy dashboard — coming soon.</p>
    </div>
  )
}
